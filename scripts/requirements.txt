# Requirements for running scripts standalone.
# These are a subset of the main requirements.txt.
# Install with: pip install -r scripts/requirements.txt

fastapi>=0.115.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
google-generativeai>=0.8.0
python-dotenv>=1.0.0
